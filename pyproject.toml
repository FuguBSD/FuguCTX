[project]
name = "fuguctx"
version = "0.0.0"
description = "FuguCTX workspace root. It holds no application code."
requires-python = ">=3.12"

[tool.uv]
package = false

[dependency-groups]
dev = [
    "ruff>=0.12",
]
